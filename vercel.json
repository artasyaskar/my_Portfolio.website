{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "."
      }
    }
  ],
  "routes": [
    { "src": "/projects.html", "dest": "/projects.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
